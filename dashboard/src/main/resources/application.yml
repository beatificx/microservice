endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true

server:
  port: 7070

logging:
  level:
    ROOT: INFO

eureka:
  client:
    serviceUrl:
      defaultZone: http://150.28.66.46:8761/eureka/
    instance:
      preferIpAddress: true
